export const baseURL = "http://test-api.edfa3ly.io"
export const defaultInitialData = { products: [], categories: [], }
export const capitalize = text => text[0].toUpperCase() + text.slice(1).toLowerCase()
